# ADR-NNNN: <标题>

> **状态**：草稿 / 已实施 / 已废止（superseded-by: ADR-NNNN）
> **作者**：<name>
> **日期**：YYYY-MM-DD
> **关联**：<相关 ADR / RFC / issue / task>
> **纪律**：按 `~/.claude/CLAUDE.md` §3.8 修 bug 归因纪律 + memory `feedback_debug_root_cause_discipline.md`

---

## 1. 背景与目标

[用 1-2 段说明：要解决什么问题 / 决策的动机]

---

## 2. 决策（Decision）

[用 1-2 句话陈述最终决策]

---

## 3. 证据（已验证假设 / 必填）

> ⚠️ **每个假设必须附独立证据**（commit hash / 实测输出 / log / 截图 / master 实现对照）

| # | 假设 | 证据 | 验证方式 |
|---|------|------|---------|
| H1 | [假设内容] | [实测输出 / commit hash] | [怎么测的] |
| H2 | [假设内容] | [实测输出 / commit hash] | [怎么测的] |
| H3 | [假设内容] | [实测输出 / commit hash] | [怎么测的] |

---

## 4. 反例（已证伪假设 / 必填）

> ⚠️ **每个被排除的假设必须附证伪过程**（为什么不是这个）
> 反例必须独立可验证（不是"应该不是" / "估计不是"）

| # | 排除假设 | 证伪原因 | 证伪方式 |
|---|---------|---------|---------|
| E1 | [假设内容] | [实测 / 代码路径 / master 对照] | [怎么排除的] |
| E2 | [假设内容] | [实测 / 代码路径 / master 对照] | [怎么排除的] |
| E3 | [假设内容] | [实测 / 代码路径 / master 对照] | [怎么排除的] |

---

## 5. 备选方案（取舍 / 必填）

> ⚠️ **至少评估 2 个备选方案 + 说明为什么选 A 不选 B/C**

| 方案 | 优点 | 缺点 | 评估 |
|------|------|------|------|
| A. 本方案 | ... | ... | ✓ |
| B. 备选 1 | ... | ... | ✗ 理由：... |
| C. 备选 2 | ... | ... | ✗ 理由：... |

---

## 6. 行为契约（可证伪 / 必填）

> ⚠️ **每条契约必须是可观测 / 可测试的**（不是"应该 / 可能"）

- ✓ **[已实测通过]**：[具体行为] —— [验证方式 + commit hash]
- ✓ **[已实测通过]**：[具体行为] —— [验证方式 + commit hash]
- ✗ **[不在本 ADR 范围]**：[具体行为]（写到 §9 后续）

---

## 7. 4 类图（必填 / ASCII）

> 按 `~/.claude/CLAUDE.md` §3.5：触发条件（≥1 文件 / 跨进程 / 改 API / 改 schema / 改部署 / 改 UI）必画

### 7.1 架构图
[模块/进程/层关系]

### 7.2 时序图
[请求-处理-响应链]

### 7.3 关系图
[实体/外键/类型引用]

### 7.4 流程图
[状态机/分支/决策]

---

## 8. TDD 流程（必填）

| Step | 状态 | 证据 |
|------|------|------|
| RED | ✓/✗/N/A | [测试名 / commit hash] |
| GREEN | ✓/✗/N/A | [实现 commit hash] |
| REFACTOR | ✓/✗/N/A | [重构 commit hash] |
| 自验证 | ✓/✗/N/A | [测试输出 / 集成输出] |

---

## 9. 后续（不在本 ADR 范围 / 必填）

> ⚠️ **本 ADR 不解决的待办必须列出来**（避免悄悄偷渡）

- [ ] **task #XX**：[简述]
- [ ] **新 issue**：[简述]

---

## 10. 真相变了怎么办（强制重写）

> ⚠️ **如果 §3-6 中任何一段被推翻，必须重写整个 ADR，不打补丁**
> 重写步骤：
> 1. 在 frontmatter 加 `superseded-by: ADR-NNNN`
> 2. 新建 ADR-NNNN 写新真相
> 3. 本文档保留作为历史归档
> 4. commit message 明确写 "重写 ADR-NNNN：旧归因 [X] → 新归因 [Y]"

---

## Debug Gate 5 项（按 §3.8 强约束 / 本 ADR 涉及 bug 修复时必填）

> ⚠️ **如果本 ADR 是 bug 修复类决策，必须填这 5 项**

- **症状**：[整段真实报错 / 用户反馈 / 实测输出，不摘要]
- **多假设**：[≥3 独立假设，每个独立可证伪]
- **修复**：[精确代码改动 + commit hash]
- **自验证**：[实测输出 + 测试结果]
- **未证明**：[哪些靠推测 / 哪些没排除 / 哪些可能后续才暴露]

---

## 自检 Checklist（提交前必过）

- [ ] §3 证据：每个假设都有独立证据（commit / 实测 / log）
- [ ] §4 反例：每个被排除的假设都有证伪过程
- [ ] §5 备选：至少评估 2 个备选方案 + 明确选择理由
- [ ] §6 行为契约：每条都可观测 / 可测试
- [ ] §7 4 类图：触发 §3.5 的改动必画
- [ ] §8 TDD：流程完整（RED-GREEN-REFACTOR-自验证）
- [ ] §9 后续：列出本 ADR 不解决的待办
- [ ] **未引用未验证的归因**（不出现"估计" / "应该" / "可能是" 等未验证用词）
- [ ] **如果涉及 bug 修复**：Debug Gate 5 项完整
- [ ] **如果真相已变**：§10 重写流程已走

---

**纪律引用**：
- `~/.claude/CLAUDE.md` §3.8 修 bug 归因纪律
- memory `feedback_debug_root_cause_discipline.md`（8 条铁律）
- memory `feedback_debug_gate.md`（Debug Gate 5 项格式）
- memory `feedback_zero_tolerance_fake_green.md`（假绿零容忍）