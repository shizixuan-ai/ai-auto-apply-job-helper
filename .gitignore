# Dependencies
node_modules/

# Build artifacts
dist/
build/
*.tsbuildinfo

# Test artifacts
coverage/
.vitest-cache/

# Environment — NEVER commit real secrets
.env
.env.*

# Runtime data (Browser profile / cookies / Feishu cache)
.bapply/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# Audit scratch files (sub-agent artifacts)
.audit-*

# CLAUDE config (project-local secrets/state)
.env.claude/

# Claude Code local state (per-user runtime, not project source)
.claude/
.claude-task.md
.claude_context.md

# Literal tilde directory (shell ~ expansion artifact, contains .boss-chrome/)
~/

# Hook 备份（Write hook 自动创建 xx.copy.example，不应 commit）
*.copy.example
*.copy.md

# 用户真实简历（个人隐私，不 commit；项目保留 简历.md.example 模板）
简历.md

